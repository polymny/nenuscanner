{
  "name": "nenu-scanner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "bundle": "esbuild ts/main.ts --bundle --minify --sourcemap --target=firefox58 --outfile=server/static/calibration-visualiser.js",
    "build": "npm run typecheck && npm run bundle",
    "dev": "esbuild ts/main.ts --bundle --sourcemap --target=firefox58 --outfile=server/static/calibration-visualiser.js --watch",
    "test": "echo \"No JS tests\" && exit 0"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/three": "^0.165.0",
    "esbuild": "^0.24.0",
    "three": "^0.165.0",
    "typescript": "^5.6.3"
  }
}
